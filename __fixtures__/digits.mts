/**
 * @file Fixtures - digits
 * @module fixtures/digits
 */

import { codes } from '@flex-development/mark-util-symbol'
import type { Code } from '@flex-development/mark/parse'

/**
 * Digits.
 *
 * @const {[NonNullable<Code>][]} digits
 */
const digits: [NonNullable<Code>][] = [
  [codes.digit0],
  [codes.digit1],
  [codes.digit2],
  [codes.digit3],
  [codes.digit4],
  [codes.digit5],
  [codes.digit6],
  [codes.digit7],
  [codes.digit8],
  [codes.digit9]
]

export default digits
