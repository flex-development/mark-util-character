/**
 * @file Fixtures - lower
 * @module fixtures/lower
 */

import { codes } from '@flex-development/mark-util-symbol'
import type { Code } from '@flex-development/mark/parse'

/**
 * Lowercase letters.
 *
 * @const {[NonNullable<Code>][]} lower
 */
const lower: [NonNullable<Code>][] = [
  [codes.lowercaseA],
  [codes.lowercaseB],
  [codes.lowercaseC],
  [codes.lowercaseD],
  [codes.lowercaseE],
  [codes.lowercaseF],
  [codes.lowercaseG],
  [codes.lowercaseH],
  [codes.lowercaseI],
  [codes.lowercaseJ],
  [codes.lowercaseK],
  [codes.lowercaseL],
  [codes.lowercaseM],
  [codes.lowercaseN],
  [codes.lowercaseO],
  [codes.lowercaseP],
  [codes.lowercaseQ],
  [codes.lowercaseR],
  [codes.lowercaseS],
  [codes.lowercaseT],
  [codes.lowercaseU],
  [codes.lowercaseV],
  [codes.lowercaseW],
  [codes.lowercaseX],
  [codes.lowercaseY],
  [codes.lowercaseZ]
]

export default lower
